export enum UmpireType {
  onfield = 'on-field',
  reserve = 'reserve',
  offfield = 'off-field',
  none = '',
}

export enum UmpireSubType {
  BOWLER_END = 'bowler end',
  SQUARE_LEG = 'square leg',
  NONE = 'none',
}